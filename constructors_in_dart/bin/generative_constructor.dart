class Student {
  String name = "Ammar";
  int age = 21;

  // Initialization of Generative Constructor

  Student(this.name, this.age);

  // if any constructor is not declared the dart's default is generative constructor
}
